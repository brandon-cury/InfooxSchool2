<ul>
    {% for item in menu %}
        <li>
            <a href="{{ path('app_books_category', {'c': item.slug}) }} ">
                <h2>{{ item.name }}</h2>
            </a>
        </li>
    {% endfor %}
</ul>
